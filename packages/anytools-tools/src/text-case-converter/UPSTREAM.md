# Upstream Attribution: text-case-converter

- [`change-case`](https://github.com/blakeembrey/change-case) — MIT — the de-facto JS case-conversion library, handles acronyms (`XMLHttpRequest` → `xml_http_request`) correctly.

Wrappers expose 14 case types in a unified API.
